# csis390-lyonslab6

# Why do we need id and name  in form elements?
IDs are for using JavaScript and CSS on the element. The name attribute is used in the HTTP request sent by your browser to the server as a variable name. 
